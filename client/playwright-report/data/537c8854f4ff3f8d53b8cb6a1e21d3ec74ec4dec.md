# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: websocket.spec.ts >> WebSocket Connection >> should show connecting state initially
- Location: e2e/websocket.spec.ts:24:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('text=/Connecting|Disconnected/')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('text=/Connecting|Disconnected/')

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('WebSocket Connection', () => {
  4  |   test('should attempt WebSocket connection on load', async ({ page }) => {
  5  |     await page.goto('/');
  6  |     
  7  |     // Listen for console messages
  8  |     const consoleMessages: string[] = [];
  9  |     page.on('console', msg => {
  10 |       consoleMessages.push(msg.text());
  11 |     });
  12 |     
  13 |     // Wait a bit for connection attempt
  14 |     await page.waitForTimeout(2000);
  15 |     
  16 |     // Check that connection was attempted
  17 |     const hasConnectionLog = consoleMessages.some(msg => 
  18 |       msg.includes('Connected') || msg.includes('Connecting') || msg.includes('WebSocket')
  19 |     );
  20 |     
  21 |     expect(hasConnectionLog).toBe(true);
  22 |   });
  23 | 
  24 |   test('should show connecting state initially', async ({ page }) => {
  25 |     await page.goto('/');
  26 |     
  27 |     // Wait for initial state
  28 |     await page.waitForTimeout(1000);
  29 |     
  30 |     // Check for connecting or disconnected state
  31 |     const connectionIndicator = page.locator('text=/Connecting|Disconnected/');
> 32 |     await expect(connectionIndicator).toBeVisible();
     |                                       ^ Error: expect(locator).toBeVisible() failed
  33 |   });
  34 | });
  35 | 
```